services:
  - type: web
    name: repo-evaluator
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 5000
      - key: GITHUB_TOKEN
        sync: false
